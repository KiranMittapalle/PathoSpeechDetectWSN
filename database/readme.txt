create a file with name 'metadata.xslx' containing two columns. 
First column with header 'SPKID' which contains unique speaker ids and second column with header 'class' which contains corresponding class labels (0 -> healthy and 1-> patient)
